
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Force a final redeployment to resolve persistent permission errors.
    // This rule allows universal read and write access to all documents.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
