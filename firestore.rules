
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Forcing another redeployment to fix persistent permission errors.
    // This rule allows universal read and write access to all documents.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
